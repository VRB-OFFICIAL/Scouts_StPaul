{
  "rules": {
    "troopTrackerData": {
      ".read": "auth != null",
      ".write": "auth != null",

      "groups": {
        ".validate": "newData.hasChildren()"
      },
      "attendance": {
        ".validate": "newData.exists()"
      },

      "$other": {
        ".validate": false
      }
    },

    "$other": {
      ".read": false,
      ".write": false
    }
  }
}
